@using ShadcnBlazor.Extras.Common

<WButton OnClick="Work1Async">Testy</WButton>

@code
{

    private async Task Work1Async(WButton button)
    {
        await Task.Delay(2000);
        await button.SetTextAsync("Doing long task");
        await Task.Delay(4000);
    }
}
